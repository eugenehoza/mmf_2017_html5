Пример выборки данных из таблицы tbl1
Пример выборки данных из таблицы tbl1 с условием на колонку а, где значение > 1 и < 0
Пример выборки данных из таблицы tbl1 с условием на колонку b, где значение > 1 или < 0
Пример выборки данных из таблицы tbl1 с условием на колонку а > 1 и на колонку с < 0
Пример выборки данных из таблиц tbl1 и tbl2, где tbl1.id == tbl2.id
Пример выборки данных из таблицы tbl1 с сортировкой по полю d по убыванию
Пример выборки среднего значения из поля e таблицы tbl1 с группировкой по полю d
Пример выборки данных из таблицы tbl1 с условием на колонку а, где значение начинается с подстроки "abc"
Пример выборки данных из таблицы tbl1 с условием на колонку а, где значение заканчивется подстрокой "abc"
Пример выборки данных из таблицы tbl1 с условием на колонку а, где значение содержит подстроку "abc"
Пример выборки данных из таблицы tbl1 с условием на колонку а, где значение от 'a' до 'f' включительно (с использованием in)
Разница между delete и truncate
Перечислить constraints (минимум 3). Описать каждое.
Функция NVL, пример
Отличия union от union all
Что такое sql subquery?
Что такое sequence?
Что такое view?
Функции работы со строками (минимум 3)
Что такое index?
Что делает функция trim?
В каком случае применяется where, а в каком Having?
Объяснить суть inner join (можно пример)
Функция INSTR, пример
Принцип работы wildcard
Пример distinct выборки
Пример where in выборки
Запрос на кол-во строк в таблице
Что такое dml
Что такое ddl