Пример комментариев в JS //, /* */
Литерал regexp {
var re = /паттерн/флаг;
var re = new RegExp('паттерн', флаг); }
С чего могут начинаться переменные в JS __, #
Зарезервированные слова в JS (минимум 5) (var, let, else, if, for)
Написать изменяемые и неизменяемые типы (symbol, string, number, boolean, undefined, null - neizm, ost - izm)
Функции объекта Math (минимум 5) (cos, sin, abc, min, max)
Функции объекта Date (минимум 5) (getDate(), getDay(), getTime(), getFullYear(), getHours())
Как задать перенос в строке? /n
Результат null == undefined true
Перечислить глобальные свойства глобального объекта (ducument.all-getElementById-getElementsByClass-body-head)
Перечислить глобальные функции глобального объекта (Document.createElement-createEvent-createRange-createTextNode-getElementsByClassName)
Пример функции-конструкторы глобального объекта 
Сконвертировать undefined в строку/число/bool/object {
undefined,
NaN,
false,
[object Object],
}
Сконвертировать null в строку/число/bool/object {
$ node test.js
null,
0,
false,
[object Object],
}
Что такое область видимости и подъем
Пример задания объекта с двумя свойствами 
Описать способы обращения к свойствам объекта .test, ["test"]
Как узнать тип переменной? typeof 
Результатом оператора будет значение true, если левое значение представляет собой имя свойства объекта, указанного справа. Какой это оператор? in
Реультат выполнения (a = b) == 0 false
Что делает eval()?
Как в функции включить строгий режим? "use strict";
Что такое тернарный оператор
Пример условных инструкции (минимум 2) if switch 
Перечислить инструкции циклов (минимум 2) for, while
Перечислить инструкции переходов (минимум 4) break, return, continue, throw
Перечислить инструкции объявления (минимум 2) var, let
Синтаксис for/in for(a in b)
Перечислить атрибуты свойства объекта (минимум 3) value,writable,set,get
Перечислить варианты создания объекта (минимум 2) var a = new Object(), var a = {name: 0, email: 0};
Перечислить методы чтения и записи свойств set, get; Object.getOwnPropertyDescriptor(circle, 'area').set
Как сериализовать объект? user = JSON.parse(user);
Методы класса Object (минимум 2+) Object.create, Object.values
Как узнать длину массива? arr.length
Перечислить способы создания массива (минимум 2+)
Что такое разреженные массивы?
Перечислить Методы класса Array (минимум 3+) arr.push, arr.pop, arr.slice
Перечислить Методы класса  5 (2+) (forEach(), map, filter)
Какой результат выполнения двух опрераций Array.isArray([]);Array.isArray({})
В дополнение к аргументам при вызове любой функции ей передается еще одно значение, определяющее контекст вызова. Какое?
Что возвращает функция, если в ней нет return? undefined
Перечислить способы вызова функции (минимум 4)
Где ошибка: var o = new Object;?
Что такое объект Arguments в функции?
Что такое замыкание в JS?
Что такое вызываемый объект?
Что такое грубое определение типа?
